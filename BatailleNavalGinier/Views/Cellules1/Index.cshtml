@model IEnumerable<BatailleNavalGinier.Models.Cellule>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdGame)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Xcoords)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.y_coords)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.is_boat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.is_hit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdGame)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Xcoords)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.y_coords)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.is_boat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.is_hit)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
